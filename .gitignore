# Claude Code agent worktrees — isolated copies, not user content
.claude/worktrees/
.claude/settings.local.json

# Tijdelijk hulpbestand voor mobiele screenshots (headless Chrome)
_shot.html

# Meetpagina's (headless Chrome, getComputedStyle-metingen)
_meet*.html
